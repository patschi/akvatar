"""Test package for akvatar.

Kept as a real package (rather than a bare directory) so test modules can
import shared helpers via ``from tests.helpers import ...`` regardless of the
directory pytest is invoked from.
"""
